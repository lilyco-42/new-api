{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the Agent window to use the explicitly registered local GitHub CLI commands.",
  "windows": ["main"],
  "permissions": ["core:default"],
  "remote": {
    "urls": ["https://api.lain42.top/*"]
  }
}
